# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET    /$path<.*> controllers.ReverseProxyController.reverseProxy(path)
POST   /$path<.*> controllers.ReverseProxyController.reverseProxy(path)
HEAD   /$path<.*> controllers.ReverseProxyController.reverseProxy(path)
DELETE /$path<.*> controllers.ReverseProxyController.reverseProxy(path)
